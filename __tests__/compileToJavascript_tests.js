import chai from 'chai'
const expect = chai.expect
import compile from '../compileToJavascript'

describe('the whole thing', () => {
    it('should load', () => {
        expect(true).to.be.true
    })
})
